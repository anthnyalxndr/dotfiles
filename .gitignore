# Bare dotfiles git dir + its bootstrap backups — never track
.dotfiles/
.dotfiles-backup-*/

# Machine-local override file (secrets / per-host) — never track
.config/shell/local

# OS / editor cruft
.DS_Store
*~
.*.sw[a-p]
.~lock.*#

# Machine/account state — never track
.config/gh/hosts.yml
.config/gh/config.yml

# Binary blobs
*.tar.gz
