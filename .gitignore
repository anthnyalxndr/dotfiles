# Bare dotfiles git dir — never track
.dotfiles/

# OS / editor cruft
.DS_Store
*~
.*.sw[a-p]
.~lock.*#

# Machine/account state — never track
.config/gh/hosts.yml
.config/gh/config.yml

# Binary blobs
*.tar.gz
